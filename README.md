# Santiago de Compostela – web-santiago (Portfolio)

Este repositorio contiene una página web básica y accesible en **HTML5** dedicada a la ciudad de **Santiago de Compostela**. Fue creada como parte de un proyecto práctico para aprender los fundamentos del desarrollo web.

Puedes ver la página publicada aquí:
👉 **https://octavioaguero.github.io/web-santiago/** 

---

## 📌 Contenido del proyecto
- `index.html` — Página principal desarrollada en HTML5.
- `/images/` — Carpeta que contiene las imágenes utilizadas en el sitio.

---

## 🧩 Tecnologías utilizadas
- **HTML5**
- **CSS básico interno** para el diseño
- Uso de etiquetas semánticas: `<header>`, `<main>`, `<section>`, `<footer>`
- Elementos accesibles:
  - `alt` en imágenes
  - `lang="es"` en el elemento `<html>`
  - Skip link para accesibilidad: `Saltar al contenido`

---

## ✔️ Requisitos cumplidos
Este proyecto cumple con los siguientes requisitos fundamentales:

- `<!DOCTYPE html>`
- `<html lang="es">`
- `<meta charset="UTF-8">`
- Uso de **encabezados** (`<h1>`, `<h2>`, etc.)
- **Párrafos** (`<p>`) 
- **Imágenes** (`<img>` con `alt` descriptivo)
- **Enlaces** (`<a href="">`)
- **Listas** (`<ul>`, `<li>`)
- **Secciones semánticas** (`<header>`, `<main>`, `<footer>`, `<nav>`, `<article>`, `<section>`, `<aside>`)

---

## 🗺️ Mapa interactivo
La página incluye un espacio preparado para insertar un mapa de Google Maps o OpenStreetMap mediante `<iframe>`.

---

## 📂 Estructura del repositorio
```bash
web-santiago/
├── pagsantiago2.html
└── imagenes/
    ├── catedrales.jpg
    └── (otras imágenes opcionales)
```

---
## 📞 Autor
Proyecto creado por **Octavio Agüero**.

---

## 🎯 Próximas mejoras 
- Añadir estilos con CSS externo.
- Hacer la página completamente responsive.
- Añadir información turística adicional (gastronomía, rutas, alojamiento).
- Crear una navegación más completa.

---

